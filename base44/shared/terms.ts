// Terms of Use version — single source of truth for the backend.
// Bump TERMS_VERSION whenever the Terms of Use change; every user must then
// re-accept the updated terms before continuing to use the platform
// (the acceptance is re-checked against this version on each session).
// Keep the version in src/lib/compliance.js in sync — it drives the frontend gate.
export const TERMS_VERSION = "1.0";